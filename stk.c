#include "monty.h"
/**
 * m_stack - prints the top
 *
 * @head: head
 * @counter: line_number
 * Return: no return
*/
void m_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
